# Night Report — 2026-07-08

## Status: BLOQUEADO — infraestrutura de specs ausente

O processo do Nightly Builder exige, como passo 1, ler `PLAN.md` na raiz do
repositório para identificar a SPEC ATIVA, e como passo 2, ler `CLAUDE.md`
para as regras do projeto. Nenhum dos dois arquivos existe neste repositório:

- `PLAN.md` — não encontrado (raiz nem em nenhum subdiretório)
- `CLAUDE.md` — não encontrado
- Nenhum diretório de specs (`specs/`, `docs/specs/`, etc.) encontrado
- Nenhuma branch `claude/spec-NNN` existe (local ou remota) — apenas
  `main` e `claude/magical-heisenberg-ydgrgp` (a branch de trabalho atual)
- `.claude/night-reports/` também não existia — foi criado agora para
  registrar este relatório

## O que foi verificado

- `git log --all` mostra histórico normal do MVP (auth, dashboard, wallet,
  redesign visual) — nenhum commit anterior referencia specs numeradas
  (`spec-001`, etc.) ou um processo de nightly build.
- `README.md` descreve o produto (Restaurante Growth Suite) mas não
  referencia PLAN.md nem um fluxo de specs.

## Por que parei

Sem `PLAN.md` não há como identificar qual é a "spec ativa", e sem specs
não há critérios de aceite para guiar um incremento verificável. Implementar
algo às cegas violaria a regra "nunca tocar em código fora do escopo da
spec" — não dá pra saber o escopo sem a spec.

## Ação necessária do founder

Para o Nightly Builder funcionar, é preciso criar na raiz do repo:

1. `PLAN.md` com a lista de specs e qual está marcada como ativa
2. `CLAUDE.md` com as regras gerais do projeto
3. Ao menos uma spec completa (ex. `specs/001-*.md`) com critérios de
   aceite verificáveis, para que o branch `claude/spec-001` possa ser
   criado a partir de `main`

Nenhuma alteração de código foi feita nesta execução — apenas este
relatório.
